@model IEnumerable<SAGM.Data.Entities.Sat_TipoComprobante>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoComprobante)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoComprobanteDesc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoComprobante)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoComprobanteDesc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoComprobanteId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoComprobanteId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoComprobanteId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
